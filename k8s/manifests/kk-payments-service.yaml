apiVersion: v1
kind: Service
metadata:
  name: kk-payments-service
  labels:
    app: kk-payments
spec:
  type: LoadBalancer
  selector:
    app: kk-payments 
  ports:
    - protocol: TCP
      port: 8067       
      targetPort: 8067   
